# Copy this file to .env locally and fill in real values only on your machine.
# Never commit .env — it stays out of Docker via .dockerignore.

GEMINI_API_KEY=
HF_TOKEN=
GEMINI_MODEL=gemini-2.5-flash
